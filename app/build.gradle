buildscript {
    repositories {
        mavenLocal()
    }
    dependencies {
        //classpath 'com.example:plugin:0.1-SNAPSHOT'
    }
}
apply plugin: 'java'

repositories {
    mavenLocal()
}

dependencies {
    compile 'com.example:plugin:0.1-SNAPSHOT'
}
