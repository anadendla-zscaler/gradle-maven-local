jar {
	// Comment this line to get build to work
	classifier = "jdk18"
    zip64 = true
}

publishing {
    publications {
        maven(MavenPublication) {
        	artifactId = "myLib"
            groupId = "com.example"
            version = "1.1"
            from components.java
        }
    }
}

tasks.publishToMavenLocal.finalizedBy(showMavenLocal)
